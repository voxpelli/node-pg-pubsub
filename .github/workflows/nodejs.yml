env: {}

# FILE GENERATED WITH: npx ghat voxpelli/ghatemplates/nodejs-pg
# SOURCE: https://github.com/voxpelli/ghatemplates
# OPTIONS: {"set":["jobs.test.strategy.matrix.postgres_version=[13,12,11,10,9.6,9.4]"]}

name: Node CI
on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
jobs:
  test:
    name: >-
      Test on node ${{ matrix.node_version }} and ${{ matrix.os }} with Postgres
      ${{ matrix.postgres_version }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        node_version:
          - 14
          - 16
          - 17
        os:
          - ubuntu-latest
        postgres_version:
          - 13
          - 12
          - 11
          - 10
          - 9.6
          - 9.4
    services:
      postgres:
        image: postgres:${{ matrix.postgres_version }}
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: postgres
        ports:
          - 5432/tcp
        options: >-
          --health-cmd pg_isready --health-interval 10s --health-timeout 5s
          --health-retries 5
    steps:
      - uses: actions/checkout@v2
      - name: Use Node.js ${{ matrix.node_version }}
        uses: actions/setup-node@v2
        with:
          node-version: ${{ matrix.node_version }}
      - name: npm install/ci
        run: >-
          [ -f package-lock.json ] && npm ci --ignore-scripts || npm install
          --ignore-scripts
      - run: npm run test-ci
        env:
          DATABASE_URL: "postgres://postgres:postgres@localhost:${{ job.services.postgres.ports[5432] }}/postgres"

